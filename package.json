{
  "name": "project-website",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "build": "npm --prefix frontend ci && npm --prefix frontend run build && rm -rf build && mkdir -p build && cp -r frontend/dist/* build/"
  }
}

